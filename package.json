{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "prisma:schemachange": "npx prisma migrate dev && npx prisma generate",
    "studio": "prisma studio"
  },
  "devDependencies": {
    "nuxt": "^3.0.0",
    "prisma": "^4.8.0"
  },
  "dependencies": {
    "@prisma/client": "^4.6.1",
    "@trpc/client": "^10.7.0",
    "@trpc/server": "^10.7.0",
    "mqtt": "^4.3.7",
    "prisma-trpc-generator": "^0.8.1",
    "trpc-nuxt": "^0.3.7",
    "zod": "^3.20.2"
  }
}
